# Dependencies
node_modules/
.pnp
.pnp.js

# Env — asli JANGAN pernah commit, .example boleh
.env
.env.local
.env.*.local
.env.production
.env.staging
!.env.production.example
!.env.staging.example
!apps/*/.env.example

# Build output
dist/
build/
.next/
out/

# § architecture-database.md "Migration Workflow" — file migration WAJIB
# ikut commit (bukan di-generate ulang tiap environment), jangan exclude
# folder ini. (Sempat salah exclude total di sini — baru ketauan pas
# setup CI pertama kali butuh migration buat provision test DB.)

# Logs
*.log
npm-debug.log*
bun-debug.log*

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/*
!.vscode/extensions.json
.idea/

# Test/coverage
coverage/

# Misc
*.tsbuildinfo
.turbo/

# Claude Code session-local state (bukan config project)
.claude/scheduled_tasks.lock

# Saved reference docs (Accurate API HTML exports, etc.) — local only, not for repo
docs/referencehtml/
